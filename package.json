{
  "name": "web-soundboard",
  "version": "1.0.0",
  "private": true,
  "description": "Web soundboard with RTMP out for muxshed, Companion HTTP API, and PWA remote control",
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "dev": "concurrently -n server,client -c cyan,magenta \"npm run dev -w server\" \"npm run dev -w client\"",
    "build": "npm run build -w client && npm run build -w server",
    "start": "npm run start -w server",
    "generate-samples": "node scripts/generate-samples.mjs"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "engines": {
    "node": ">=20"
  }
}
